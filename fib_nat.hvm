@add = λ{
  #ZER: λb. b;
  #SUC: λa. λb. @add(a,#SUC{b})
}

@fib = λ{
  #ZER: #ZER;
  #SUC: λ{
    #ZER: #SUC{#ZER}
    #SUC: λp.
      ! P &L = p;
      @add(@fib(#SUC{P₀}), @fib(P₁))
  }
}

@u32 = λ{
  #ZER: λn. n
  #SUC: λp. λn. @u32(p,1+n)
}

@main = @u32(@fib(34n),0)

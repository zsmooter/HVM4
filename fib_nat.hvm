@add = λ{
  #ZER: λb. b;
  #SUC: λa. λb. #SUC{@add(a,b)}
}

@fib = λ{
  #ZER: #ZER;
  #SUC: λ{
    #ZER: #SUC{#ZER}
    #SUC: λp.
      ! P &L = p;
      @add(@fib(#SUC{P₀}), @fib(P₁))
  }
}

@u32 = λ{
  #ZER: 0
  #SUC: λp. 1 + @u32(p)
}

@main = @u32(@fib(30n))
